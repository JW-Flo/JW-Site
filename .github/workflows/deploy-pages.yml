name: Deploy Pages (jw-immersive) [DEPRECATED]

on:
    push:
        branches: [main]
        paths:
            - "apps/jw-immersive/**"
            - ".github/workflows/deploy-pages.yml"

jobs:
    build-and-deploy:
        if: false # DEPRECATED: Astro jw-immersive deployment disabled; console worker replaces this.
        runs-on: ubuntu-latest
        permissions:
            contents: read
        steps:
            - name: Deprecated Notice
              run: |
                  echo 'jw-immersive Astro deployment is deprecated and disabled.'
                echo 'Use atlasit-console worker deployment instead.'
