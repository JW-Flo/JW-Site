# ESLint Ignore Migration

This project has migrated to ESLint v9 flat config. The legacy .eslintignore file was removed in favor of the `ignores` property in eslint.config.js.

If you intended to ignore additional paths, add them to the `ignores` array in `eslint.config.js`.
