
<section class="relative overflow-hidden py-24 bg-gradient-to-br from-slate-900 via-slate-800 to-slate-900">
  <!-- Immersive animated background shapes -->
  <div class="absolute inset-0 pointer-events-none z-0">
    <div class="absolute top-[-80px] left-[-80px] w-[320px] h-[320px] bg-gradient-to-br from-blue-500/30 to-purple-500/10 rounded-full blur-3xl animate-pulse-slow"></div>
    <div class="absolute bottom-[-100px] right-[-100px] w-[400px] h-[400px] bg-gradient-to-tr from-pink-500/20 to-blue-400/10 rounded-full blur-3xl animate-pulse-slower"></div>
  </div>
  <div class="relative z-10 max-w-5xl mx-auto flex flex-col md:flex-row items-center gap-12 px-4">
    <!-- Profile Image -->
    <div class="flex-shrink-0 flex flex-col items-center md:items-start">
      <img src="/profile-placeholder.jpg" alt="Joe Whittle" class="w-40 h-40 rounded-full shadow-lg border-4 border-blue-400/40 object-cover mb-4 animate-fade-in" loading="eager" />
      <span class="block text-sm text-slate-400 mt-2 md:ml-2">Cybersecurity Engineer</span>
    </div>
    <!-- Hero Content -->
    <div class="flex-1 text-center md:text-left">
      <h1 class="text-5xl md:text-6xl font-extrabold text-white mb-4 leading-tight">
        <span class="text-blue-400">Joe Whittle</span>
      </h1>
      <p class="text-2xl text-slate-200 mb-6 max-w-2xl animate-fade-in-delay">
        Protecting people, systems, and ideas—<span class="text-neon-green font-semibold">one challenge at a time</span>.<br/>
        <span class="text-slate-400 text-lg">Engineer. Veteran. Builder. Storyteller.</span>
      </p>
      <div class="flex flex-wrap gap-4 justify-center md:justify-start mb-8 animate-fade-in-delay-2">
        <a href="/demo" class="bg-blue-600 hover:bg-blue-700 text-white font-bold py-3 px-6 rounded-lg transition-colors shadow-lg hover:scale-105">View Demo</a>
        <a href="/workflows" class="bg-purple-600 hover:bg-purple-700 text-white font-bold py-3 px-6 rounded-lg transition-colors shadow-lg hover:scale-105">Live Systems</a>
        <a href="/enhanced-security-scanner" class="bg-gradient-to-r from-purple-600 to-pink-600 text-white font-bold py-3 px-6 rounded-lg transition-colors shadow-lg hover:scale-105">🚀 Security Scanner</a>
      </div>
      <!-- Stats -->
      <div class="grid grid-cols-1 sm:grid-cols-3 gap-6 text-center md:text-left">
        <div class="bg-slate-800/80 p-4 rounded-lg shadow-md">
          <div class="text-3xl font-bold text-green-400 mb-1">50+</div>
          <div class="text-sm text-slate-400">Incidents Resolved</div>
        </div>
        <div class="bg-slate-800/80 p-4 rounded-lg shadow-md">
          <div class="text-3xl font-bold text-blue-400 mb-1">40%</div>
          <div class="text-sm text-slate-400">Response Time</div>
        </div>
        <div class="bg-slate-800/80 p-4 rounded-lg shadow-md">
          <div class="text-3xl font-bold text-purple-400 mb-1">98%</div>
          <div class="text-sm text-slate-400">Closure Rate</div>
        </div>
      </div>
    </div>
  </div>
  <style>
    .animate-pulse-slow { animation: pulse 4s cubic-bezier(0.4,0,0.6,1) infinite; }
    .animate-pulse-slower { animation: pulse 7s cubic-bezier(0.4,0,0.6,1) infinite; }
    .animate-fade-in { animation: fadeInUp 1.2s cubic-bezier(0.4,0,0.2,1) 0.1s both; }
    .animate-fade-in-delay { animation: fadeInUp 1.2s cubic-bezier(0.4,0,0.2,1) 0.5s both; }
    .animate-fade-in-delay-2 { animation: fadeInUp 1.2s cubic-bezier(0.4,0,0.2,1) 0.9s both; }
    @keyframes fadeInUp {
      from { opacity: 0; transform: translateY(40px); }
      to { opacity: 1; transform: translateY(0); }
    }
  </style>
</section>
