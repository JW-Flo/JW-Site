---
---
<section class="text-center py-12 md:py-20 geometric-bg relative overflow-hidden">
  <div class="absolute inset-0 bg-gradient-to-br from-transparent via-neon-blue/5 to-transparent animate-pulse"></div>
  <div class="absolute inset-0 bg-[radial-gradient(circle_at_30%_20%,rgba(0,255,255,0.1),transparent_50%)]"></div>
  <div class="absolute inset-0 bg-[radial-gradient(circle_at_70%_80%,rgba(255,0,255,0.1),transparent_50%)]"></div>

  <div class="relative z-10">
    <h1 class="text-4xl md:text-6xl font-bold font-sans glitch relative mb-4" data-text="Joe Whittle">
      <span class="text-neon-pink">Joe</span> <span class="text-neon-cyan">Whittle</span>
    </h1>
    <p id="typing-text" class="text-xl md:text-2xl text-neon-pink mt-4 font-mono mb-6"></p>
    <p class="text-base md:text-lg text-slate-300 max-w-2xl mx-auto mt-6 px-4 leading-relaxed">
      Cybersecurity engineer blending edgy tech with measurable impact. Protecting digital frontiers through innovation and expertise.
      <span class="text-neon-green font-semibold">Explore my interactive portfolio featuring retro arcade games and automated security workflows.</span>
    </p>

    <div class="flex flex-col sm:flex-row justify-center space-y-4 sm:space-y-0 sm:space-x-4 mt-8 px-4">
      <a href="/demo" class="px-8 py-4 bg-neon-blue text-dark font-bold rounded-lg hover-neon transition pulse-neon text-lg shadow-[0_0_20px_rgba(0,255,255,0.3)]">
        🎮 Play Games & Explore
      </a>
      <a href="/projects" class="px-8 py-4 border-2 border-neon-pink text-neon-pink font-bold rounded-lg hover:bg-neon-pink hover:text-dark transition text-lg hover:shadow-[0_0_20px_rgba(255,0,255,0.3)]">
        🚀 View Projects
      </a>
    </div>

    <!-- Quick Stats -->
    <div class="grid grid-cols-2 md:grid-cols-4 gap-6 mt-12 max-w-4xl mx-auto px-4">
      <div class="bg-dark/50 backdrop-blur-sm border border-neon-green/30 rounded-lg p-4 hover:border-neon-green transition-colors">
        <div class="text-2xl font-bold text-neon-green mb-1">50+</div>
        <div class="text-sm text-slate-300">Incidents Resolved</div>
      </div>
      <div class="bg-dark/50 backdrop-blur-sm border border-neon-blue/30 rounded-lg p-4 hover:border-neon-blue transition-colors">
        <div class="text-2xl font-bold text-neon-blue mb-1">40%</div>
        <div class="text-sm text-slate-300">Faster Response</div>
      </div>
      <div class="bg-dark/50 backdrop-blur-sm border border-neon-pink/30 rounded-lg p-4 hover:border-neon-pink transition-colors">
        <div class="text-2xl font-bold text-neon-pink mb-1">98%</div>
        <div class="text-sm text-slate-300">Vulnerability Closure</div>
      </div>
      <div class="bg-dark/50 backdrop-blur-sm border border-neon-cyan/30 rounded-lg p-4 hover:border-neon-cyan transition-colors">
        <div class="text-2xl font-bold text-neon-cyan mb-1">24/7</div>
        <div class="text-sm text-slate-300">Security Monitoring</div>
      </div>
    </div>

    <!-- Scroll Indicator -->
    <div class="absolute bottom-8 left-1/2 transform -translate-x-1/2 animate-bounce">
      <svg class="w-6 h-6 text-neon-green" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 14l-7 7m0 0l-7-7m7 7V3"></path>
      </svg>
    </div>
  </div>
</section>

<script>
  const text = "Building secure systems, one step at a time";
  const typingText = document.getElementById('typing-text');
  let index = 0;

  function typeWriter() {
    if (index < text.length && typingText) {
      typingText.innerHTML += text.charAt(index);
      index++;
      setTimeout(typeWriter, 100);
    }
  }

  // Start typing animation when page loads
  document.addEventListener('DOMContentLoaded', () => {
    setTimeout(typeWriter, 1000);
  });
</script>
